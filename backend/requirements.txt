

# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0        # includes websockets, watchfiles, httptools
gunicorn==21.2.0                 # production process manager

# ── ASGI / HTTP ────────────────────────────────────────────────────────────
httptools==0.6.1                 # fast HTTP parser (used by uvicorn[standard])
# uvloop==0.19.0                   # fast event loop (used by uvicorn[standard])

# ── Database ───────────────────────────────────────────────────────────────
sqlalchemy==2.0.30               # ORM
psycopg2-binary==2.9.9           # PostgreSQL driver (binary = no compile needed)
alembic==1.13.1                  # schema migrations

# ── Auth / Security ────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0 # JWT encoding/decoding
passlib[bcrypt]==1.7.4  
bcrypt==4.0.1         # password hashing (bcrypt)
python-multipart==0.0.9          # multipart form data + file uploads (UploadFile)

# ── Configuration ──────────────────────────────────────────────────────────
pydantic==2.7.1                  # data validation (FastAPI uses this internally)
pydantic-settings==2.2.1         # Settings class with .env file support
python-dotenv==1.0.1             # .env loader (backup to pydantic-settings)
email-validator==2.1.1           # validates EmailStr fields in Pydantic schemas

# ── Email (OTP / password reset) ───────────────────────────────────────────
# Uses Python stdlib smtplib — no extra package needed.
# SMTP config in backend/.env: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS

# ── Utilities ──────────────────────────────────────────────────────────────
anyio==4.3.0                     # async compatibility layer
starlette==0.37.2                # FastAPI dependency (keep pinned with fastapi)

# ── Dev / Testing (comment out in production if desired) ───────────────────
# httpx==0.27.0                  # async HTTP client for testing
# pytest==8.2.0
# pytest-asyncio==0.23.6
